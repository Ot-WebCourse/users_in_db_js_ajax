Додано перевірку чи існує користувач з таким логіном, оскільки він має бути унікальним (див. форму реєстрації). Це виконується за допомогою AJAX-запиту під час введення логіна (це запит на сервері обробляє окремий контролер - JsonController).
Додано валідацію форми реєстації (перевірку коректності введених даних) на клієнтській стороні (JS) перед відправкою даних на сервер.

...
Дані про користувачів зберігаються в базі даних H2.
Ідентифікація поточного користувача виконується на основі даних, збережених в сессії.

Для зручності забезпечено доступ до консолі бази даних H2 (для перегляду схеми БД  і вмісту)

Містить: 
- форму реєстрації, 
- форму авторизації користувачів, 
- особисту сторінку користувача,
- сторінку зі списком користувачів.




